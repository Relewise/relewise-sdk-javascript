name: Node.js CI

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

env:
  node-version: '16'

jobs:
  test:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: ./lib
    strategy:
      fail-fast: true
    steps:
      - uses: actions/checkout@v3
      - name: Use Node.js ${{ env.node-version }}
        uses: actions/setup-node@v3
        with:
          node-version: ${{ env.node-version }}
      - run: npm ci
        name: Install dependencies
      - run: npm run gen-api 
        name: Generate API type defs
      - run: npm run build
        name: Build
      - run: npm run build:types
        name: Build Types
      - run: npm test
        name: Unit tests
      - run: npm run integration-test --DATASET_ID=${{secrets.INTEGRATIONTESTS_DATASET_ID}} --API_KEY=${{secrets.INTEGRATIONTESTS_API_KEY}}
        env:
          DATASET_ID: ${{secrets.INTEGRATIONTESTS_DATASET_ID}}
          API_KEY: ${{secrets.INTEGRATIONTESTS_API_KEY}}
        name: Integration tests