{
  "name": "app",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "set NODE_ENV=development && jest ./tests/unit-tests",
    "integration-test": "set NODE_ENV=development && jest ./tests/integration-tests",
    "start": "tsc && set NODE_ENV=development && node --experimental-specifier-resolution=node --trace-warnings index.js "
  },
  "type": "module",
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/jest": "^28.1.6",
    "@types/node": "^17.0.23",
    "jest": "^28.1.3",
    "ts-jest": "^28.0.7",
    "ts-node": "^10.9.1",
    "typescript": "^4.7.4"
  }
}
